import '../../../core/widgets/placeholder_page.dart';

class SettingsScreen extends PlaceholderPage {
  const SettingsScreen({super.key})
      : super(
          title: 'Settings',
          description: 'Local-only preferences, exports, and accessibility controls.',
        );
}

