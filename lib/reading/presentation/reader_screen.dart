import '../../../core/widgets/placeholder_page.dart';

class ReaderScreen extends PlaceholderPage {
  const ReaderScreen({super.key})
      : super(
          title: 'Reader',
          description: 'Manual, paced, RSVP, skim, and scan modes will run here.',
        );
}

