enum PassageDifficulty {
  easy,
  standard,
  hard,
  technical,
}

enum PassageType {
  fiction,
  nonFiction,
  technical,
  academic,
  news,
  workplace,
  examStyle,
}

enum PassageSource {
  official,
  imported,
}

enum ReadingMode {
  manual,
  paced,
  rsvp,
  skim,
  scan,
  deepRead,
}

enum AttemptQualificationStatus {
  qualified,
  unqualified,
  interrupted,
  incomplete,
}

enum QuestionType {
  multipleChoice,
  mainIdea,
  detailRecall,
  inference,
  vocabularyInContext,
  writtenSummary,
}

enum TrainingGoal {
  school,
  work,
  exam,
  personalLearning,
  generalImprovement,
}

