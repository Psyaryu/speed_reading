import '../../../core/widgets/placeholder_page.dart';

class ProgressScreen extends PlaceholderPage {
  const ProgressScreen({super.key})
      : super(
          title: 'Progress',
          description: 'Progress toward 800 WPM will always include comprehension.',
        );
}

