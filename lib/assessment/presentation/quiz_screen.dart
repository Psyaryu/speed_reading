import '../../../core/widgets/placeholder_page.dart';

class QuizScreen extends PlaceholderPage {
  const QuizScreen({super.key})
      : super(
          title: 'Quiz',
          description: 'Comprehension checks will verify speed gains.',
        );
}

